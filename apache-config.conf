# ============================================================
# BuildProcure Apache + PHP-FPM Configuration
# ============================================================

# --- Redirect HTTP to HTTPS ---
<VirtualHost *:80>
    ServerName buildprocure.com
    Redirect permanent / https://buildprocure.com/
</VirtualHost>

# --- Main HTTPS VirtualHost ---
<VirtualHost *:443>
    ServerName buildprocure.com

    DocumentRoot /var/www/html

    SSLEngine on
    SSLCertificateFile "/usr/local/apache2/conf/ssl/buildprocure.com_combined.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/ssl/_.buildprocure.com_private_key.key"

    <Directory /var/www/html>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.php index.html
    </Directory>

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://127.0.0.1:9000"
    </FilesMatch>

    ProxyPreserveHost On
    ProxyPass "/simplesamlphp/" "http://simplesamlphp:80/"
    ProxyPassReverse "/simplesamlphp/" "http://simplesamlphp:80/"

    ErrorLog /var/log/apache2/buildprocure_error.log
    CustomLog /var/log/apache2/buildprocure_access.log combined

    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-Content-Type-Options nosniff

    SetEnv HTTPS on
    RequestHeader set X-Forwarded-Host %{HTTP_HOST}s
    RequestHeader set X-Forwarded-Proto https
    RequestHeader set X-Forwarded-Port 443
    # Ensure Authorization header is passed to PHP-FPM
    SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

</VirtualHost>
# --- Redirect hub.buildprocure.com HTTP to HTTPS ---
<VirtualHost *:80>
    ServerName hub.buildprocure.com
    Redirect permanent / https://hub.buildprocure.com/
</VirtualHost>

# --- Proxy hub.buildprocure.com to ilifes-items-frontend ---
<VirtualHost *:443>
    ServerName hub.buildprocure.com

    SSLEngine on
    # Using your existing wildcard or combined certs
    SSLCertificateFile "/usr/local/apache2/conf/ssl/buildprocure.com_combined.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/ssl/_.buildprocure.com_private_key.key"

    ProxyPreserveHost On
    # 1. Catch all requests starting with /api and send to backend container
    ProxyPass /api http://ilifes-items-backend:80/api
    ProxyPassReverse /api http://ilifes-items-backend:80/api

    # 2. Catch everything else and send to frontend container
    ProxyPass / http://ilifes-items-frontend:80/
    ProxyPassReverse / http://ilifes-items-frontend:80/

    # Optional: Match the SSL headers your app might expect
    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-Port "443"

    ErrorLog /var/log/apache2/hub_error.log
    CustomLog /var/log/apache2/hub_access.log combined
</VirtualHost>
# --- Redirect hirenow.buildprocure.com HTTP to HTTPS ---
<VirtualHost *:80>
    ServerName hirenow.buildprocure.com
    Redirect permanent / https://hirenow.buildprocure.com/
</VirtualHost>

# --- Proxy hirenow.buildprocure.com to hirenow container ---
<VirtualHost *:443>
    ServerName hirenow.buildprocure.com

    SSLEngine on
    SSLCertificateFile "/usr/local/apache2/conf/ssl/buildprocure.com_combined.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/ssl/_.buildprocure.com_private_key.key"

    ProxyPreserveHost On
    ProxyPass / http://hirenow:80/
    ProxyPassReverse / http://hirenow:80/

    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-Port "443"

    ErrorLog /var/log/apache2/hirenow_error.log
    CustomLog /var/log/apache2/hirenow_access.log combined

    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-Content-Type-Options nosniff
</VirtualHost>
# --- Redirect ncees.buildprocure.com HTTP to HTTPS ---
<VirtualHost *:80>
    ServerName ncees.buildprocure.com
    Redirect permanent / https://ncees.buildprocure.com/
</VirtualHost>

# --- Proxy ncees.buildprocure.com to NCEES FE Practice container ---
<VirtualHost *:443>
    ServerName ncees.buildprocure.com

    SSLEngine on
    SSLCertificateFile "/usr/local/apache2/conf/ssl/buildprocure.com_combined.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/ssl/_.buildprocure.com_private_key.key"

    ProxyPreserveHost On
    ProxyPass / http://ncees-fe-practice:80/
    ProxyPassReverse / http://ncees-fe-practice:80/

    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-Port "443"

    ErrorLog /var/log/apache2/ncees_error.log
    CustomLog /var/log/apache2/ncees_access.log combined
    CustomLog /var/log/apache2/ncees_access.log combined

    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-Content-Type-Options nosniff
</VirtualHost>